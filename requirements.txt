# Python dependencies for sync.py
# Works on Linux, macOS, and Windows.
#
# Install:
#   pip install -r requirements.txt
#
# music2emo downloads its model weights from HuggingFace automatically on first
# use and caches them in ~/.cache/huggingface/.

spotipy>=2.23.0
music2emo
numpy>=1.24.0
tqdm>=4.0.0
